<odoo>
    <data>
        <record id="view_employee_list_inherit" model="ir.ui.view">
            <field name="name">hr.employee.list.view.inherit</field>
            <field name="model">hr.employee</field>
			<field name="inherit_id" ref="hr.view_employee_tree" />
            <field name="arch" type="xml">
				<xpath expr="//field[@name='work_location_id']" position="after">
					<field name="user_on_device_ids"
                        widget="many2many_tags"
                        options="{'color_field': 'color'}"
                        optional="hide"
                        groups="hr.group_hr_user"/>
                    <field name="device_ids"
                        widget="many2many_tags"
                        options="{'color_field': 'color'}"
                        optional="hide"
                        groups="hr.group_hr_user"/>
				</xpath>
			</field>
		</record>

        <record id="action_server_upload_employee" model="ir.actions.server" >
            <field name="name">Upload Employees</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_hr_employee" />
            <field name="binding_model_id" ref="model_hr_employee" />
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_upload_employee()</field>
        </record>

    </data>
</odoo>